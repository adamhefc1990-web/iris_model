# iris_model
AI fundamentals course, exploring importing iris data frame and creating a prediction model and evaluate findings
# Iris Classifier (Decision Tree)

## Overview
End‑to‑end ML example from Digital Marketing Mastery Module → builds a decision‑tree classifier on the classic Iris dataset using scikit‑learn.
```bash
git clone https://github.com/<adamhefc1990-web>/iris_model.git
cd iris-classifier
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python src/train.py
